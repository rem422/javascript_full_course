//We use LET to declare variables that can change later during the execution of our program.

let age = 30;
// Reassigning a value to to a variable.
age = 31;
console.log(age);

//We use CONST to declare variables that can not be change at any point in the future.

const birthYear = 2000;
// birthYear = 2001;
// console.log(birthYear);

// We can not declare empty const variables.

// const job;

// VAR is the old way of defining variables prior to ES6.

var job  =  'Programmer';
job = 'Teacher';
console.log(job);

lastName = 'Simiyu';
console.log(lastName);
