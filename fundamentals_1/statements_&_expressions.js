// Expression - is a piece of code that produceses value.
//3 + 4
//1991
// true && false && !false

// Statement - is a bigger piece of code that is excecuted and does not produce a value on its own.

if (23 > 10) {
    const str = '23 is bigger';
}

console.log(` I'm ${new Date().getFullYear() - 2000} years old`);
